# Reproducible inactive templates — 2026-09-24

Read-only production inspection found all 15 populated template bodies, all inactive. The repository's schema/registry/source sequence (009, 010, 011) only recreated one populated body. `templates/hr-letters.snapshot.json` now captures the exact current database code, title, version, body, inactive flag and MD5 for each template. No employee or letter records are included. A version string containing “director-approved” is preserved source data, not independent evidence of wording or legal approval.

`node tests/build-hr-letter-template-restore.mjs` prints a reviewable SQL transaction; it does not connect to any database. Apply only after schema/registry/source setup and environment-specific release review. This generator is not an applied or timestamped production migration. It fills missing/empty bodies and handles CRLF-to-LF line endings from Windows SQL checkouts. It refuses active templates or nonempty bodies/titles/versions that differ from the snapshot, rather than overwriting them. It locks the template table during comparison and restore, retains inactive status, and leaves employee data, permission rows, letter records and issued snapshots untouched.

`node tests/hr-letter-template-restore.mjs` reconstructs the schema and initial registry/source in isolated PGlite, verifies the original one-body gap, restores all 15 exact bodies/versions/hashes, verifies a second run changes nothing, and checks rejection of changed wording and active templates. It is included in `npm test --prefix tests`. All tests pass. No live restore or template activation was performed.

Production deployment remains on hold. Before a release, package the reviewed generated SQL in the normal migration process and verify it in the full staging environment. The combined LOC_LOI source still hardcodes MYEDEN GROUP SDN. BHD.; do not treat it as a verified template for other employing entities. Company-name resolution, salary-source clarification, wording review and full-app acceptance remain separate release gates. The snapshot intentionally preserves existing wording rather than silently revising it.
